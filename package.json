{
  "name": "tg",
  "version": "0.1.0",
  "description": "Telegram MTProto user-account automation (teleproto).",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20.12"
  },
  "scripts": {
    "telegram:auth": "tsx scripts/telegram/auth.ts",
    "typecheck": "tsc --noEmit",
    "test": "node --import tsx --test test/*.test.ts"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  },
  "dependencies": {
    "teleproto": "^1.229.0"
  }
}
